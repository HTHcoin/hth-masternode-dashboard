# Copy to .env and fill in. Backend is READ-ONLY — it never needs a wallet passphrase.
PORT=8710
RPC_URL=http://127.0.0.1:9998/
RPC_USER=hthrpc
RPC_PASSWORD=changeme_match_helpthehomeless_conf
# Origins allowed to call the API (the static frontend + local dev):
CORS_ORIGINS=https://stake.hth.foundation,http://localhost:8080
CACHE_TTL_MS=15000
